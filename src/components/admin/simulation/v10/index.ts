import { WorkspaceRegistry } from '../../../../lib/kernel/WorkspaceRegistry';
import { SimulationWorkspace } from './SimulationWorkspace';
import { OptimizationWorkspace } from './OptimizationWorkspace';

export function registerSimulationWorkspaces(registry: WorkspaceRegistry) {
  // Add registration logic here if needed
}

export { SimulationWorkspace, OptimizationWorkspace };
